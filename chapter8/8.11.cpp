#include<iostream>
#include<fstream>
#include<sstream>
#include<vector>
using namespace std;

int main(int argc, char const *argv[]){
  string line,word;
  vector<string> name;
  istringstream record;
  while(getline(cin,line)){
    record.str(line);
  }
}